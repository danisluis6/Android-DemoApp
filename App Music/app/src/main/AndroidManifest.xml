<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.enclaveit.app_meida">
    <!-- Definition permission to using in application -->

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="19"
        />
    <!-- Running API Lower than API current can running App Android -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_launch"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.TestSQLiteActivity"></activity>
    </application>

</manifest>